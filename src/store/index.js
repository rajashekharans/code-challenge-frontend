import Vue from 'vue';
import Vuex from 'vuex';

import geoJsonSource from '@/store/modules/geoJsonSource';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    geoJsonSource,
  },
});
